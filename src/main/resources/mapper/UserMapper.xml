<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.database.paperms.mapper.UserMapper">

    <select id="getById" resultType="com.database.paperms.entity.User">
        select * from user where user_id = #{id}
    </select>

    <insert id="insertUser" useGeneratedKeys="true" keyProperty="userId">
        insert into user(user_name,user_account,user_password) values(#{userName}, #{userAccount}, #{userPassword})
    </insert>

</mapper>